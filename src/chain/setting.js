let properties;

function setProperties(chainProperties) {
  properties = chainProperties;
}

function getProperties() {
  return properties;
}

module.exports = {
  setProperties,
  getProperties,
}
